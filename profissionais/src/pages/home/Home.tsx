// Esqueleto da Home do profissional. O conteúdo (resumo de aplicações,
// estoque, estatísticas etc.) ainda não foi definido — esta Page existe
// só para a navegação (Login → Home) já funcionar. O desenvolvimento
// completo fica para uma etapa futura.
function Home() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-800">Home</h1>
      <p className="mt-1 text-sm text-gray-500">
        Esta página ainda será desenvolvida.
      </p>
    </div>
  );
}

export default Home;
